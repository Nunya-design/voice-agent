# SDR Voice App

A basic outbound voice SDR application using Twilio, Vercel (Next.js), and eventually OpenAI.

## Getting Started

1. Install dependencies
2. Set up your `.env.local`
3. Run `npm run dev` to start the local server
4. Use the dashboard to initiate calls